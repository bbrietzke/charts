
1. Should have created/updated {{ len .Values.nfsVolumes }} NFS volumes.
2. Should have created/updated {{ len .Values.localVolumes }} local volumes
3. You can view the persistent volumes with: kubectl get persistentVolumes
