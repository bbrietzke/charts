apiVersion: v1
kind: Service
metadata:
  name: {{ include "volumes.fullname" . }}
  labels:
    {{- include "volumes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "volumes.selectorLabels" . | nindent 4 }}
